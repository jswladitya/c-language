#include <stdio.h>
#include <string.h>

int main()
{
    char *st = "Aditya";
    strlen(st); //used to print the length of a string excluding the null character
    printf("The length of string st is %d", strlen(st));
    return 0;
}