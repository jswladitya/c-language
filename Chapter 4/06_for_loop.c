#include<stdio.h>

int main(){
    for(int a = 0; a < 10 ; a++){
       printf("The value of a is %d \n", a); 
    //printf("The value of a is %d \n", a+1);
    //pehle 1 print & so on
    }

    //first a = 0 print hoga. fir increment hoga means a = 1 banega & if the condition is true 1 print hoga and so on
    return 0;
}