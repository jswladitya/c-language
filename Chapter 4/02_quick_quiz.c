#include <stdio.h>
// Write a program to print natural no. from  10 to 20
int main()
{
    int i=0;
    while(i <= 20)
    {
        if (i >= 10)
        {
            printf("The Value of i is %d\n",i);
        }
        i++;

    }
        return 0;
}