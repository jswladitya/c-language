#include <stdio.h>

int main()
{
    int a = 2;
    // int b 4.5 // Not recommended because 4.5 double no. so we have to use float

    float b = 4.5;
    char c = 'c';
    int d = 34;
    printf("The value of b is %f \n", b);
    printf("The value of a is %d \n", a);
    printf("The value of a is %c \n", c);
    printf("Sum of a & d is %d", a + d); // That is how we can do calculations

    return 0;
}