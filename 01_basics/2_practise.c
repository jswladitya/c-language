#include <stdio.h>
// Program to find area of circle
int main()
{
    int r = 3;
    float pi = 3.14;
    printf("the area of circle is %f\n", pi * r * r);
    printf("the volume of circle is %f\n", pi * r * r * r);
    return 0;
}